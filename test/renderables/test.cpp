#include<catch2/catch_test_macros.hpp>

import std;
import renderables;

SCENARIO("Timer")
{
	GIVEN("a renderable manager")
	{
		WHEN("instantiated")
		{
			THEN("test suite should work")
			{
				CHECK(true);
			}
		}
	}
}

